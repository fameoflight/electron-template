/**
 * Entity Classes - Static Entity Imports
 *
 * ⚠️ DO NOT EDIT THIS FILE DIRECTLY ⚠️
 * This file is auto-generated from schemas/*.json
 * 
 * Purpose: Single source of truth for entity import paths.
 * Separates path definitions from loading logic for better maintainability.
 *
 * Benefits:
 * - Static: All imports resolved at compile time
 * - Type-safe: Direct TypeScript imports
 * - Simple: No dynamic loading complexity
 * - Performant: No runtime import overhead
 */

// Base entities
import { User } from '@main/base/db/User.js';

// Domain entities
{{#each entities}}
import { {{name}} } from './entities/{{filename}}.js';
{{/each}}

/**
 * Static entity classes object
 *
 * Provides direct access to all entity classes without dynamic loading.
 * This is the preferred way to access entities in the application.
 */
export const entityClasses = {
  User,{{#each entities}}
  {{name}},{{/each}}
} as const;

/**
 * Type definition for entity name keys
 */
export type EntityName = keyof typeof entityClasses;

/**
 * Type for all entity classes
 */
export type EntityClasses = typeof entityClasses;

/**
 * Get a specific entity class by name
 *
 * @param name - Entity name
 * @returns Entity class
 *
 * @example
 * ```typescript
 * const User = getEntityClass('User');
 * const user = new User();
 * ```
 */
export function getEntityClass<K extends EntityName>(name: K): EntityClasses[K] {
  return entityClasses[name];
}

/**
 * Get all entity classes as an object
 *
 * @returns All entity classes
 *
 * @example
 * ```typescript
 * const entities = getAllEntityClasses();
 * const { User, Chat, Job } = entities;
 * ```
 */
export function getAllEntityClasses(): EntityClasses {
  return entityClasses;
}

/**
 * Get all entity classes as an array
 *
 * Useful for TypeORM DataSource configuration and other contexts
 * that expect an array of entity classes.
 *
 * @returns Array of all entity classes
 *
 * @example
 * ```typescript
 * const entities = getEntitiesArray();
 * const dataSource = new DataSource({
 *   entities,
 *   // ... other config
 * });
 * ```
 */
export function getEntitiesArray(): EntityClasses[keyof EntityClasses][] {
  return Object.values(entityClasses);
}

/**
 * Get all entity names as an array
 *
 * @returns Array of all entity names
 *
 * @example
 * ```typescript
 * const allNames = getAllEntityNames(); // ['User', 'Job', 'Connection', ...]
 * ```
 */
export function getAllEntityNames(): EntityName[] {
  return Object.keys(entityClasses) as EntityName[];
}